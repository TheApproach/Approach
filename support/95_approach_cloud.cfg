network:
    version: 2
    renderer: NetworkManager
    ethernets:
        nameservers:
            addresses:
            - "{{ orchestra_dns[0] }}"
            - "{{ orchestra_dns[1] }}"
            search: [corp,private]